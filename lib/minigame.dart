import 'package:flutter/material.dart';

// placeholder
class MinigamePage extends StatelessWidget {
  const MinigamePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Minigame Page")),
      body: const Center(child: Text("This is the Minigame Page")),
    );
  }
}

// log out ?
// navbar